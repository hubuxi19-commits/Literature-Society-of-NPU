# 文社网站待完成更新任务

> 状态基准：截至 2026-08-12，发布1（账号与生产安全）、发布2（稳定数据读取）、发布3（遗留修复）、发布3（版本与批注）、发布4（私密社交与站内通知）、发布5（治理与管理员编辑能力）已上线并复验通过。本文件列出检测到的全部未完成项及其规划，供后续分轮实施。
> 工作目录：`C:\Users\legion\Documents\Codex\2026-08-02\qin\work\wenyuan-community-upgrade`
> 分支：`codex/wenyuan-community-upgrade`　HEAD：`1707447`（已快进发布 `main`）

## 一、当前基线（已完成）

| 发布 | 内容 | 状态 |
|---|---|---|
| 发布1 | 账号与生产安全：笔名周改迁移、找回邮箱绑定/验证/更换、一次性验证码、密码找回、Turnstile、限速、模糊化找回响应、`write_gate off→warn` | 已上线 |
| 发布2 | 稳定数据读取：`browse_works`/`browse_discussions` 分页 RPC、`pg_trgm` 正文搜索、讨论页分页、桌面再读十篇、移动端临近末尾预取、元信息 13px/移动 16px/触控 44px | 已上线 |
| 发布3 | 发布2 遗留问题修复（#4–#10）：分页返回正文、计数排除删评、预取失败重试、搜索双触发等 | 已上线 |
| 发布3 | 版本与批注：不可变版本历史、编辑产生新版本、恢复旧版不删历史、固定版本文本选区批注（含码点偏移与 trim 契约对齐） | 已上线（2026-08-09） |
| 发布4 | 私密社交与站内通知：关注、收藏、评论点赞、聚合站内通知、未读角标、我的关注/收藏/粉丝页 | 已上线（2026-08-11） |

- 生产站点：`https://hubuxi19-commits.github.io/Literature-Society-of-NPU/`
- 生产 Supabase ref：`odfjxtzgekhiaktzaxas`　staging ref：`rcrqosnbkojaarppvcac`
- 生产 `write_gate` = **`warn`**（未切 `enforce`）
- 已实现表：`profiles`、`works`、`likes`、`comments`、`site_settings`、`account_recovery_emails`、`account_action_tokens`、`auth_rate_limits`
- 已实现 Edge Functions：`account-email`、`password-recovery`（唯一可用 `service_role` 的位置）

## 二、未完成总览

| 待办 | 内容 | 状态 |
|---|---|---|
| 待办0 | 生产 `write_gate` 从 `warn` 切 `enforce` | 未切换（发布1 收尾） |
| 发布4 | 私密社交与站内通知 | 已上线复验（2026-08-11） |
| 发布5 | 治理与管理员编辑能力 | 已上线复验（2026-08-12） |
| 收尾 | 数据库备份演练、CSP/Referrer Policy、隐私说明、最终 RLS 复核与冒烟测试 | 未开始 |

设计依据：`docs/superpowers/specs/2026-08-02-community-platform-upgrade-design.md` 的阶段 C/D/E 与用户确认的数据库与隐私边界方案。

---

## 待办0：生产 write_gate 切 enforce（发布1 收尾）

**目标：** 未验证找回邮箱的账号在数据库层禁止写操作，前端跳转 + RLS/RPC 双重拦截。

**前置条件（全部满足才执行）：**
- [ ] `warn` 已运行数天，老用户陆续完成找回邮箱绑定
- [ ] 生产抽查验证矩阵 A1–A9 全部通过（登录、绑定、掩码、重复邮箱拒绝、防枚举、验证码次数、限速、找回后身份不变、日志干净）
- [ ] 负责人在生产站点实测绑定一次找回邮箱成功
- [ ] 负责人书面批准切 `enforce`

**执行：** 在**生产 SQL Editor**（确认 ref `odfjxtzgekhiaktzaxas`）执行 `docs/production-enforce-stepup.md` 第 4 步：
```sql
update public.site_settings
set value = jsonb_set(value, '{write_gate}', '"enforce"'::jsonb, true)
where key = 'account_security';
```

**切换后验证矩阵 B1–B9：** 未登录/未验证成员/已验证成员/管理员未验证/管理员已验证 五种身份的真实浏览器会话验证；页面隐藏按钮不等于数据库拒绝，必要时用控制台直接调用 RPC 验证被拒。

**回滚（单条 SQL 随时可用）：** `'enforce'` 改回 `'warn'`。

---

## 发布3：版本与批注

### 数据库
- 新增表 `work_versions`：`work_id`、单调递增 `version_number`、标题/摘要/分类/正文快照、修改说明、`restored_from_version_id`、创建者、创建时间
- `works` 增加当前版本指针（保留身份、作者、状态、推荐状态）
- 新增表 `comment_quotes`：评论 ID、作品版本 ID、引用原文、起止位置、创建时间（引用原文不随后续版本变化）
- 迁移现有作品为第 1 版（`work_versions` 回填，`works.current_version_id` 指向第 1 版）
- 受保护 RPC（同一事务）：
  - 创建新版本：作者必须填写简短修改说明；携带当前版本号，版本已变化时拒绝覆盖
  - 恢复旧版本：复制旧内容生成新的最新版本，记录来源版本，不删除任何历史
  - 创建引用批注：记录当前可见版本号
- RLS：已发布历史对所有读者公开；只有作者能为自己的作品创建新版本；管理员不能冒充作者改正文；引用不可伪造

### 前端
- 阅读页只显示最新版本；历史版本入口公开展示全部版本号、时间、修改说明、正文快照
- 作者在历史版本页“恢复此版本”；普通读者只能查看历史
- 写作台改为“产生新版本”，保存前填写修改说明
- 读者选择当前可见版本中的连续文字创建公开批注；桌面端正文旁显示批注入口，移动端正文下方按引用顺序集中显示
- 批注同时进入作品讨论与全站讨论页

### 测试
- 版本号单调递增、修改不覆盖历史、恢复旧版生成新版本且历史不丢失
- 引用批注保存正确版本、原文和位置；修改最新版不改变历史引用
- 迁移前后作品数量、作者 ID、正文快照、版本数量核对一致
- 四类身份（匿名/未验证/已验证/管理员）RLS 验证；版本与评论版本关联不可伪造

---

## 发布4：私密社交与站内消息

### 数据库
- 新增表 `notifications`：接收者、触发者、事件类型、作品/版本/评论关联、精简载荷、创建时间、已读时间；RLS 只允许接收者读取和更新自己的通知
- 新增表 `follows`：关注者 + 被关注作者唯一关系；仅关注者本人可读自己的关注列表；被关注作者只能看聚合数量
- 新增表 `bookmarks`：用户 + 作品唯一关系；仅收藏者本人可读；收藏数量不公开、不参与排行榜
- 新增表 `comment_likes`：用户 + 评论唯一“有启发”关系；公开查询只返回聚合数量，不返回点赞者名单
- 通知触发：评论、回复、评论获赞、优质评论推荐、关注作者新作（短时间连续投稿合并提醒）；自己触发的动作不通知自己
- 幂等/唯一约束消除重复通知、关注、收藏、点赞
- 所有新表写策略增加 `is_recovery_email_verified()` 条件（仅前端隐藏按钮不算，RLS 与受保护 RPC 同时阻止未验证账号）

### 前端
- 关注、收藏、评论“有启发”按钮（位于正文之后或边栏，不长期占据阅读区域）
- 站内消息页：未读数量、单条已读、全部已读
- 私密关注列表页、私密收藏页（仅本人可见）
- 取消关注/收藏不通知对方
- 找回邮箱**永不接收**社区互动消息

### 测试
- 通知、关注、收藏、点赞越权读取拦截（非所有者读不到）
- 聚合数量公开、名单不公开
- 自己触发不通知自己；重复提交不产生重复通知/关系
- 未验证账号的所有写请求被数据库拒绝

---

## 发布5：治理与管理员编辑能力

> **已上线复验（2026-08-12）**：规划任务 1–15 全部完成并提交；生产迁移部署与线上复验已执行。全量 `npm test` 全绿（单元/DB/静态 320 + 浏览器桌面/移动/账号安全/社交/治理五流程）。生产 Supabase（ref `odfjxtzgekhiaktzaxas`）已事务内执行 `20260811_governance_and_admin.sql`：4 新表 RLS 全部启用、9 个新 RPC 齐全、`upsert_notification` 为唯一 6 参签名、事件类型约束含 `comment_highlight`/`moderation_outcome`。前端 `69cfa0c`→`1707447` 快进推送 `main`（经 SSH 通道），GitHub Pages 构建完成；线上只读冒烟通过（首页 10 篇作品、阅读页、`#/admin` 路由无 JS 错误、Supabase 请求全部 200）。

### 数据库
- 新增表 `work_editorial_notes`、`comment_highlights`：只有管理员可新增/修改，保存推荐理由、编辑点评、管理员 UUID、时间；普通用户只能读取针对公开作品的公开内容
- 新增表 `reports`：举报目标、类型、说明、举报者、状态；举报者身份仅管理员可见，被举报者只能看到最终处置结果
- 新增表 `moderation_actions`：管理员处置写入独立审计记录；前端无权删除审计记录
- 受保护 RPC：举报、处置（必须选择明确状态并写内部说明）、设置推荐理由、优质评论推荐（触发通知评论作者）、编辑点评
- 所有管理员同时拥有编辑能力，不新增编辑角色
- 新表写策略同样接入 `is_recovery_email_verified()` 与 `is_admin()` 校验

### 前端
- 举报入口（作品、评论、账号公开资料）
- 管理员处置台 + 审计记录展示
- 编辑点评独立于普通评论，标注管理员身份和时间
- 投稿页显示原创、引用来源、排版检查、参与互评的提醒清单；**只引导，不阻止发布**
- 完成整站视觉与无障碍复核：移动正文 16px/行高 1.9、关键元信息不低于 13px、触控目标最小 44px、弱化文字对比度、未读朱砂标记、焦点状态；保留米白/墨黑/暗红/宋体/留白/细线气质，不引入玻璃拟态、表情导航、商业卡片流、长期侧栏

### 测试
- 普通成员不能写审计、推荐理由、点评；只有管理员可
- 举报者不能读他人举报；被举报者读不到举报者身份
- 未验证账号（含管理员未验证）写操作被拒
- 桌面与移动端真实截图对比度/触控/焦点检查

---

## 收尾：安全清单与上线收尾

README「正式上线前安全清单」未勾选项，对应到本规划：

- [ ] 数据库备份与恢复演练（发布前必须，生产数据备份 → 计数 → 抽样验证）
- [ ] 配置 Content Security Policy、Referrer Policy 等安全响应头（GitHub Pages 自定义响应头支持有限，评估 Cloudflare 或其他静态托管层）
- [ ] 制定隐私说明：公开资料、日志、内容保留与删除规则
- [ ] 最终生产迁移/RLS 复核：确认所有新表 RLS `true`、`write_gate` 状态、前端只有 URL + publishable key
- [ ] 线上登录、投稿、评论、通知、版本、批注、举报冒烟测试
- [ ] 服务端学号身份映射（Edge Function）为可选后续项

---

## 每次发布的执行流程（已确认，逐次遵守）

迁移文件审查 → 测试环境验证 → 完整自动化测试 → 生产迁移确认 → 前端发布 → 线上冒烟测试。

不把所有数据库变化塞进一次高风险迁移；每个子系统完成测试与审查后再进入下一项。

## 协作红线

- 新增测试验证真实行为，不以正则检查源码文字作为主要测试；发布前秘密扫描仍保留
- 向 staging 写 migration、设 secrets、部署函数、建虚构用户、发 Brevo 测试邮件前，必须先停下请求 staging-only 授权
- 生产、`main`、GitHub Pages 未经负责人明确批准不动
- 续接会话先完整阅读 `outputs/文苑项目续接包-2026-08-02.md` 与进度账本
- 禁止把完整找回邮箱放进公开查询/日志/通知；前端永不出现 `service_role` 或任何密钥
